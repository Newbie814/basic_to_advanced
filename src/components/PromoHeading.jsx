import React from 'react';

const PromoHeading = ({ heading, callToAction }) => {
  return (
    <>
      {/* // <h1>{children}</h1>
      // <h2>{children}</h2> */}
      {console.log(heading, callToAction)}
    </>
  );
};

export default PromoHeading;
